import java.lang.ProcessBuilder;
import java.lang.Process;
import java.io.InputStreamReader;
import java.io.BufferedReader;

createEvent_powershell_script = "C:/Users/OPMuser/Documents/micromanager/mmgr-opm-scripts/powershell_event/createErrorEvent.ps1";

String[] commandListCreate = {"powershell.exe", "-Command", createEvent_powershell_script, "-verb", "runAs", "OPMUser"};  
ProcessBuilder pbCreate = new ProcessBuilder(commandListCreate);
pbCreate.redirectErrorStream(true);
Process pCreate = pbCreate.start();

// pCreate.waitFor();

BufferedReader r = new BufferedReader(new InputStreamReader(pCreate.getInputStream()));
String line;
while (true) {
   line = r.readLine();
   print(line);
   if (line == null) { break; }
   print(line);
}